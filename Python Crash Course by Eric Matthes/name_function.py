# A code used for checking the test outputs.

def get_formatted_name(firstname, middle, lastname):
    """Generate a neatly formatted full name from first and last name."""

    fullname = f"{firstname} {middle} {lastname}"
    return fullname.title()
